{
  "name": "@ecuabyte/cortex-core",
  "version": "0.9.4",
  "description": "Local-first engineering state store and continuity primitives for Cortex",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm && bunx tsc --declaration --emitDeclarationOnly --outDir dist",
    "dev": "bun build src/index.ts --outdir dist --target node --format esm --watch",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "keywords": [
    "engineering-state",
    "handoff",
    "verification",
    "provenance",
    "coding-agents",
    "context",
    "agent",
    "mcp",
    "sqlite"
  ],
  "author": "EcuaByte <hello@ecuabyte.com> (https://github.com/EcuaByte-lat)",
  "license": "MIT",
  "homepage": "https://github.com/EcuaByte-lat/Cortex#readme",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/EcuaByte-lat/Cortex.git",
    "directory": "packages/core"
  }
}
